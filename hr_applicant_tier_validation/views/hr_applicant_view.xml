<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2019 Open Source Integrators
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="hr_applicant_form_tier" model="ir.ui.view">
        <field name="name">hr.applicant.form - validate</field>
        <field name="model">hr.applicant</field>
        <field name="inherit_id" ref="hr_recruitment.hr_applicant_view_form" />
        <field name="arch" type="xml">
            <button name="create_employee_from_applicant" position="before">
                <button
                    name="request_validation"
                    string="Request Validation"
                    attrs="{'invisible': ['|','|',('need_validation', '!=', True),('rejected','=',True),('state','in',['proposal'])]}"
                    type="object"
                />
                <button
                    name="restart_validation"
                    string="Restart Validation"
                    attrs="{'invisible': ['|',('review_ids', '=', []),('state','not in',['processing'])]}"
                    type="object"
                />
            </button>
            <header position="after">
                <field name="need_validation" invisible="1" />
                <field name="validated" invisible="1" />
                <field name="rejected" invisible="1" />
                <div
                    class="alert alert-warning"
                    role="alert"
                    attrs="{'invisible': ['|', '|', '|',
                     ('validated', '=', True), ('state', 'not in', ['processing']),
                     ('rejected', '=', True), ('review_ids', '=', [])]}"
                    style="margin-bottom:0px;"
                >
                    <p><i class="fa fa-info-circle" />This Job needs to be
                        validated.
                        <field name="can_review" invisible="1" />
                        <button
                            name="validate_tier"
                            string="Validate"
                            attrs="{'invisible': [('can_review', '=', False)]}"
                            type="object"
                            class="oe_inline oe_button btn-success"
                            icon="fa-thumbs-up"
                        />
                        <button
                            name="reject_tier"
                            string="Reject"
                            attrs="{'invisible': [('can_review', '=', False)]}"
                            type="object"
                            class="btn-icon btn-danger"
                            icon="fa-thumbs-down"
                        />
                    </p>
                </div>
                <div
                    class="alert alert-success"
                    role="alert"
                    attrs="{'invisible': ['|', '|', ('validated', '!=', True), ('state', 'not in', ['processing']), ('review_ids', '=', [])]}"
                    style="margin-bottom:0px;"
                >
                    <p><i class="fa fa-thumbs-up" /> Operation has been <b
                        >validated</b>!</p>
                </div>
                <div
                    class="alert alert-danger"
                    role="alert"
                    attrs="{'invisible': ['|', '|', ('rejected', '!=', True), ('state', 'not in', ['processing']), ('review_ids', '=', [])]}"
                    style="margin-bottom:0px;"
                >
                    <p><i class="fa fa-thumbs-down" /> Operation has been <b
                        >rejected</b>.</p>
                </div>
            </header>



            <xpath expr="//field[@name='source_id']" position="after">
                <field name="state" invisible="1"/>
                <!--field name="stage_id" /-->
            </xpath>
            <xpath expr="//button[@name='create_employee_from_applicant']" position="attributes">

                <attribute name="attrs">{'invisible': [('state' ,'in', ['processing'])]}</attribute>
            </xpath>
            <xpath expr="//form/div[hasclass('oe_chatter')]" position="before">
                <field
                    name="review_ids"
                    widget="tier_validation"
                    attrs="{'invisible':[('review_ids', '=', [])]}"
                />
            </xpath>
        </field>
    </record>
    <!--<record id="view_job_filter" model="ir.ui.view">
        <field name="name">hr.job.view - hr_job_validation</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr.view_job_filter" />
        <field name="arch" type="xml">
            <filter name="my_hr_job_filter" position="after">
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids','in',uid), ('state', 'not in', ['recruit'])]"
                    help="My Sales to review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validated', '=', True)]"
                    help="SOs validated and ready to be confirmed"
                />
            </filter>
        </field>
    </record>-->
</odoo>
